export interface CeremonyItem {
  id: string;
  title: string;
  musicName: string;
  url: string;
  cover: string;
}

export interface MusicTrack {
  id: string;
  title: string;
  author: string;
  url: string;
  cover: string;
}
